1. База данных feedback состоит из следующих таблиц: users, feedback;
2. Таблица users имеет следующие поля: id, login, password (md5), email, privileges (1 - admin, 0 - обычный пользователь (по умолчанию))
3. Таблица feedback: id, text, date, is_changed (1 - изменен админом, 0 - нет (по умолчанию)), image_name (NULL),
                     visibility (visible по умолчанию, disable - не видимый, юзеру с правами админа видны все).

Users:
id - INT(10),
login - VARCHAR(30),
password - CHAR(32),
email - VARCHAR(50),
privileges - ENUM('1', '0'),
user_id - INT(10);

Feedback:
id - INT(10),
text - TEXT,
date - DATETIME,
is_changed - ENUM('1', '0'),
image_name - TEXT,
visibility - ENUM('visible', 'disable');